spring:
  cloud:
    discovery:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false
  datasource:
    url: jdbc:h2:~/testdb
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
  flyway:
    enabled: false