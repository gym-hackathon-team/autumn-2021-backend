type User {
    id: ID!
    email: String
    password: String
    firstName: String
    lastName: String
}

type Mutation {
    updateUser(id: ID!, email: String, password: String, firstName: String, lastName: String): User
}

type Query {
    getUser(id: ID): User
}